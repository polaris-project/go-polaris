// Package common creates a set of commonly used definitions and helper method.
package common

// DefaultDataDir is the default root directory in which polaris data is stored.
const DefaultDataDir = "data"

// DefaultDbDir is the default directory in which database files are stored.
const DefaultDbDir = "data/db"
